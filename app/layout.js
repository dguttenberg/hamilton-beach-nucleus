export const metadata = {
  title: "Hamilton Beach Brand Nucleus",
  description: "Brand intelligence system",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
